{
  "name": "rut-kit-monorepo",
  "private": true,
  "description": "Validador de RUT chileno con mensajes personalizados para JavaScript, TypeScript y Zod",
  "author": {
    "name": "Javier Gómez Contreras",
    "email": "hola@javiev.dev"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/javiev/rut-kit.git"
  },
  "bugs": {
    "url": "https://github.com/javiev/rut-kit/issues"
  },
  "homepage": "https://rut-kit.pages.dev/",
  "scripts": {
    "build": "pnpm --filter rut-kit build",
    "test": "pnpm --filter rut-kit test",
    "test:coverage": "pnpm --filter rut-kit test:coverage",
    "test:watch": "pnpm --filter rut-kit test:watch",
    "lint": "pnpm -r lint",
    "lint:fix": "pnpm -r lint:fix",
    "format": "pnpm -r format",
    "docs:dev": "pnpm --filter docs docs:dev",
    "docs:build": "pnpm --filter docs docs:build",
    "docs:preview": "pnpm --filter docs docs:preview",
    "prepare": "husky"
  },
  "lint-staged": {
    "*.{js,ts}": "biome check --write"
  },
  "engines": {
    "node": ">=18.13.0"
  },
  "devDependencies": {
    "@biomejs/biome": "2.3.11",
    "husky": "^9.1.7",
    "lint-staged": "^16.2.7"
  }
}
